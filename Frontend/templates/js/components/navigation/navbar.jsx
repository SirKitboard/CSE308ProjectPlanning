define([
    "jquery",
    "underscore",
    "react",
    "backbone"
], function($, _, React, backbone) {
    return React.createClass({
        render: function() {
            return(
                <div>
                    <h1>LOGO</h1>
                </div>
            )
        }
    });
})
